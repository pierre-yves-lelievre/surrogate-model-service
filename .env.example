# Directory where trained models are persisted
MODELS_DIR=./models

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Application version reported in /health and OpenAPI metadata
APP_VERSION=0.1.0
